name = "atm-labs-hub"
pages_build_output_dir = "."
compatibility_date = "2024-03-20"

# Route to custom domain
# The Cloudflare Pages dashboard will also need to be configured to route labs.trujillomingorance.com here.
